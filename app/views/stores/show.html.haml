- model_class = Store
.page-header
  %h1=t '.title', :default => model_class.model_name.human.titleize

%p
  %strong= model_class.human_attribute_name(:used_space) + ':'
  %br
  = @store.used_space
%p
  %strong= model_class.human_attribute_name(:total_space) + ':'
  %br
  = @store.total_space
%p
  %strong= model_class.human_attribute_name(:reception) + ':'
  %br
  = @store.reception
%p
  %strong= model_class.human_attribute_name(:dispatch) + ':'
  %br
  = @store.dispatch
%p
  %strong= model_class.human_attribute_name(:lung) + ':'
  %br
  = @store.lung

.form-actions
  = link_to t('.back', :default => t("helpers.links.back")), stores_path, :class => 'btn'
  = link_to t('.edit', :default => t("helpers.links.edit")), edit_store_path(@store), :class => 'btn'
  = link_to t('.destroy', :default => t("helpers.links.destroy")), store_path(@store), :method => "delete", :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-danger'
